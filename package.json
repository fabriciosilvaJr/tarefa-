{
  "scripts": {
    "start": "live-server --port=3000",
    "server": "json-server --watch db.json --port 5000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3",
    "live-server": "^1.2.2"
  }

}
